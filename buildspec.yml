version: 0.2

phases:
  install:
    commands:
      - echo Nothing to do in install...
  pre_build:
    commands:
      - docker build -t sample-aws-ap .
  build:
    commands:
      - echo Nothing to do in build phase...
  post_build:
    commands:
      - echo Build completed on `date`